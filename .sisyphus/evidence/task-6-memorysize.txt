TASK 6 Evidence: MemorySize IsUnknown Guard

CODE CHANGE (line 981):
Before: if plan.MemorySize.ValueInt64() != state.MemorySize.ValueInt64()
After:  if !plan.MemorySize.IsNull() && !plan.MemorySize.IsUnknown() && plan.MemorySize.ValueInt64() != state.MemorySize.ValueInt64()

VERIFICATION:
$ go build ./zstack/provider/
✓ Build successful

CRITICAL BUG PREVENTED:
When MemorySize is Unknown, ValueInt64() returns 0. Without the guard, this would:
1. Trigger comparison: 0 != state.MemorySize (always true if state > 0)
2. Send updateVmInstanceParam.Params.MemorySize = 0 to ZStack API
3. SHRINK VM MEMORY TO ZERO → VM becomes unusable

The IsNull && IsUnknown guards prevent this catastrophic data loss scenario.
