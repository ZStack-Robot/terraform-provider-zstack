---
page_title: "{{.Name}} {{.Type}} - {{.ProviderName}}"
subcategory: ""
description: |-
  {{ .Description | plainmarkdown | trimspace | prefixlines "  " }}
---

# {{.Name}} ({{.Type}})

{{ .Description }}

`template_content` is required and must contain the `ZStackTemplateFormatVersion` marker. The provider validates this before calling the ZStack API.

## Example Usage

{{tffile "examples/resources/stack_template/resource.tf"}}

{{ .SchemaMarkdown | trimspace }}

## Import

Import is supported using the following syntax:

```shell
terraform import zstack_stack_template.example <uuid>
```
